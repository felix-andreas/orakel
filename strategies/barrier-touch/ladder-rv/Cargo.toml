[package]
name = "ladderrv"
version = "0.1.0"
edition = "2021"
description = "barrier-touch/ladder-rv: backtest + live tooling for Polymarket 'Hit Price' one-touch barrier ladders"

[dependencies]
anyhow = "1"
chrono = "0.4"
reqwest = { version = "0.12", features = ["blocking", "json"] }
serde_json = "1"

[profile.release]
strip = true
